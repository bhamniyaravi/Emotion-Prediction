# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9

# ML / model serving
# NOTE: keras must be >= the version the model was saved with (3.13.2),
# otherwise load_model() fails with:
#   "Unrecognized keyword arguments passed to Embedding: {'quantization_config': None}"
numpy==2.3.5
tensorflow-cpu==2.21.0
keras==3.15.0
